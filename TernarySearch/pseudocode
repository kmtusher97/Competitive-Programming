ternarySearch( start, end ) :
    l = start
    r = end;

    for( i : 0 to 200 ) :
      l1 = ( l * 2 + r ) / 3
      l2 = ( l + 2 * r ) / 3
      // print l1, l2
      if( f( l1 ) > f( l2 ) ) 
        r = l2
      else 
        l = l1;
    end loop

    x = l
    return f(x)
